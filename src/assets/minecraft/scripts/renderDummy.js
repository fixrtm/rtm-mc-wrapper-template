var rmw2 = Packages.rmw2

// initialize rtm-mc-wrapper
rmw2.Init.init(eval);

var init = rmw2.Util.entityInit(function init (entity, model) {
    // init
})

var render = rmw2.Util.entityRender(function init (entity, pass, partialTicks) {
    // render
})
